# Claude Prompt for Generation of Hobbies Page

### Claude creation context
```Create hobbies.html following the exact same structure, styling, and component patterns as experience.html, running.html, and index.html. Reference index.html specifically for the name animation section, countdown, carousel pattern, and redirect card layout since those patterns may be reused or adapted. Use the shared styles.css file and link to a new hobbies.js file. The page should include the same navbar, footer, and sticky header.
Content will be provided as a markdown file listing favorite games, movies, and music. For each category, choose an appropriate display pattern already used in the codebase — for example flip cards for games similar to the projects section, a carousel or timeline for movies, and a different layout for music.
Use the same color palette (#f0ede8, #d0ccc7, #e8e4de, #1a1a1a), same font (Playfair Display), same Bootstrap grid system, same clamp() responsive sizing approach, and same scroll-triggered animation patterns from getBoundingClientRect(). Add section comments to the HTML. Do not introduce any new libraries, use basic boostrap and JS. Do not try to create images for any content, use place holders and I will go in and add real images. For the content of the pages, refrence claude/favorite-games.md, claude/favorite-music.md, claude/favorite-movies.md.
```

